import { Component, Input } from '@angular/core';

@Component({
    selector: 'lucide-icon',
    standalone: true,
    template: `<!-- Mock for compilation check since I can't easily see if node_modules works perfectly in edit -->`
})
export class MockLucideIcon { }
// This is just a placeholder thought, I should NOT write this. I will rely on the real module.
