import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bharatvasoya.filemanager',
  appName: 'Bharat Vasoya & Associates',
  webDir: 'dist/file-manager/browser'
};

export default config;
